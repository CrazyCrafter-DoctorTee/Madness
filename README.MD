# MADNESS
A pokemon ripoff of extremely high quality

## SETUP
In order to run this game, you just need to install the requirements, then run the madness.py file

## TESTS
Madness has a complete set of unit tests. It uses the pytest framework for the tests. They can be run using the following command:
```python -m pytest```